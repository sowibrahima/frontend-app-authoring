diff --git a/node_modules/@edx/frontend-plugin-notifications/dist/Notification/NotificationTabs.js b/node_modules/@edx/frontend-plugin-notifications/dist/Notification/NotificationTabs.js
index 13739e9..ce7d4be 100644
--- a/node_modules/@edx/frontend-plugin-notifications/dist/Notification/NotificationTabs.js
+++ b/node_modules/@edx/frontend-plugin-notifications/dist/Notification/NotificationTabs.js
@@ -1,9 +1,12 @@
 import React, { useEffect, useContext, useCallback, useRef } from 'react';
 import { Tab, Tabs } from '@openedx/paragon';
+import { useIntl } from '@edx/frontend-platform/i18n';
 import NotificationSections from './NotificationSections';
+import messages from './messages';
 import { notificationsContext } from './context/notificationsContext';
 import { useNotification } from './data/hook';
 const NotificationTabs = () => {
+  const intl = useIntl();
   const {
     appName,
     handleActiveTab,
@@ -37,14 +40,21 @@ const NotificationTabs = () => {
     defaultActiveKey: appName,
     onSelect: handleActiveTab,
     className: "px-2.5 text-primary-500 tabs position-sticky zIndex-2 bg-white"
-  }, appsId.map(app => /*#__PURE__*/React.createElement(Tab, {
+  }, appsId.map(app => {
+    const appMessage = {
+      discussion: messages.discussionApp,
+      updates: messages.updatesApp,
+      grading: messages.gradingApp
+    }[app.toLowerCase()];
+    return /*#__PURE__*/React.createElement(Tab, {
     key: app,
     eventKey: app,
-    title: app,
+    title: appMessage ? intl.formatMessage(appMessage) : app,
     notification: tabsCount[app],
     tabClassName: "pt-0 py-2 px-2.5 d-flex border-top-0 mb-0 align-items-center line-height-24 text-capitalize",
     "data-testid": `notification-tab-${app}`
-  }, appName === app && /*#__PURE__*/React.createElement(NotificationSections, null)))) : /*#__PURE__*/React.createElement(NotificationSections, null);
+  }, appName === app && /*#__PURE__*/React.createElement(NotificationSections, null));
+  })) : /*#__PURE__*/React.createElement(NotificationSections, null);
 };
 export default /*#__PURE__*/React.memo(NotificationTabs);
 //# sourceMappingURL=NotificationTabs.js.map
diff --git a/node_modules/@edx/frontend-plugin-notifications/dist/Notification/messages.js b/node_modules/@edx/frontend-plugin-notifications/dist/Notification/messages.js
index 8f8c0ef..36ceca8 100644
--- a/node_modules/@edx/frontend-plugin-notifications/dist/Notification/messages.js
+++ b/node_modules/@edx/frontend-plugin-notifications/dist/Notification/messages.js
@@ -1,5 +1,20 @@
 import { defineMessages } from '@edx/frontend-platform/i18n';
 const messages = defineMessages({
+  discussionApp: {
+    id: 'notification.app.discussion',
+    defaultMessage: 'Discussion',
+    description: 'Notification category for course discussions'
+  },
+  updatesApp: {
+    id: 'notification.app.updates',
+    defaultMessage: 'Updates',
+    description: 'Notification category for course updates'
+  },
+  gradingApp: {
+    id: 'notification.app.grading',
+    defaultMessage: 'Grading',
+    description: 'Notification category for grading activity'
+  },
   notificationTitle: {
     id: 'notification.title',
     defaultMessage: 'Notifications',
diff --git a/node_modules/@edx/frontend-plugin-notifications/src/Notification/NotificationTabs.jsx b/node_modules/@edx/frontend-plugin-notifications/src/Notification/NotificationTabs.jsx
index 59cf4ab..3603b7d 100644
--- a/node_modules/@edx/frontend-plugin-notifications/src/Notification/NotificationTabs.jsx
+++ b/node_modules/@edx/frontend-plugin-notifications/src/Notification/NotificationTabs.jsx
@@ -3,12 +3,15 @@ import React, {
 } from 'react';
 
 import { Tab, Tabs } from '@openedx/paragon';
+import { useIntl } from '@edx/frontend-platform/i18n';
 
 import NotificationSections from './NotificationSections';
+import messages from './messages';
 import { notificationsContext } from './context/notificationsContext';
 import { useNotification } from './data/hook';
 
 const NotificationTabs = () => {
+  const intl = useIntl();
   const {
     appName, handleActiveTab, tabsCount, appsId, updateNotificationData,
   } = useContext(notificationsContext);
@@ -44,18 +47,26 @@ const NotificationTabs = () => {
           onSelect={handleActiveTab}
           className="px-2.5 text-primary-500 tabs position-sticky zIndex-2 bg-white"
         >
-          {appsId.map((app) => (
-            <Tab
-              key={app}
-              eventKey={app}
-              title={app}
-              notification={tabsCount[app]}
-              tabClassName="pt-0 py-2 px-2.5 d-flex border-top-0 mb-0 align-items-center line-height-24 text-capitalize"
-              data-testid={`notification-tab-${app}`}
-            >
-              {appName === app && <NotificationSections />}
-            </Tab>
-          ))}
+          {appsId.map((app) => {
+            const appMessage = {
+              discussion: messages.discussionApp,
+              updates: messages.updatesApp,
+              grading: messages.gradingApp,
+            }[app.toLowerCase()];
+
+            return (
+              <Tab
+                key={app}
+                eventKey={app}
+                title={appMessage ? intl.formatMessage(appMessage) : app}
+                notification={tabsCount[app]}
+                tabClassName="pt-0 py-2 px-2.5 d-flex border-top-0 mb-0 align-items-center line-height-24 text-capitalize"
+                data-testid={`notification-tab-${app}`}
+              >
+                {appName === app && <NotificationSections />}
+              </Tab>
+            );
+          })}
         </Tabs>
       )
       : <NotificationSections />
diff --git a/node_modules/@edx/frontend-plugin-notifications/src/Notification/messages.js b/node_modules/@edx/frontend-plugin-notifications/src/Notification/messages.js
index 2f3d5be..62c9ef3 100644
--- a/node_modules/@edx/frontend-plugin-notifications/src/Notification/messages.js
+++ b/node_modules/@edx/frontend-plugin-notifications/src/Notification/messages.js
@@ -1,6 +1,21 @@
 import { defineMessages } from '@edx/frontend-platform/i18n';
 
 const messages = defineMessages({
+  discussionApp: {
+    id: 'notification.app.discussion',
+    defaultMessage: 'Discussion',
+    description: 'Notification category for course discussions',
+  },
+  updatesApp: {
+    id: 'notification.app.updates',
+    defaultMessage: 'Updates',
+    description: 'Notification category for course updates',
+  },
+  gradingApp: {
+    id: 'notification.app.grading',
+    defaultMessage: 'Grading',
+    description: 'Notification category for grading activity',
+  },
   notificationTitle: {
     id: 'notification.title',
     defaultMessage: 'Notifications',
